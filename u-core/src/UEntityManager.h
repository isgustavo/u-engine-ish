#pragma once
#include <memory>
#include <string>
#include <vector>
#include <map>

namespace uei
{
	//class UEntity;

	//class UEntityManager
	//{
	//public:
	//	UEntityManager() = default;
	//	//~EntityManager();

	//	void Update();

	//	std::shared_ptr<UEntity> AddEntity(const std::string& inTag);
	//	const std::vector<std::shared_ptr<UEntity>>& GetEntities();
	//	const std::vector<std::shared_ptr<UEntity>>& GetEntitiesByTag(const std::string& inTag);

	//private:
	//	std::vector<std::shared_ptr<UEntity>> entities;
	//	std::map<std::string, std::vector<std::shared_ptr<UEntity>>> entitiesMap;
	//	std::vector<std::shared_ptr<UEntity>> toAdd;
	//	size_t totalEntities;

	//	void RemoveEntities(std::vector<std::shared_ptr<UEntity>>& inEntities);
	//};
}